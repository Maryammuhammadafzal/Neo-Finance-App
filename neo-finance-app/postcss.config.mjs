const config = {
  theme: {
    extend: {
      colors: {
        primary: "#1C1F4A",   // Deep navy
        accent: "#00BFA6",    // Mint green
        text: "#F4F4F5",      // Light text color
        background: "#0F172A", // Dark blue/gray background
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
